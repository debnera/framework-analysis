# Run_7 analysis

- This run is somewhat broken (not enough Kafka partitions to suit all workers)
- Replaced by run_7b and run_7c

Compared to 6:
- Add HPA for master and workers
- One long run for each resolution, instead of one run per worker count
